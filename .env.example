# One key, for whichever vendor `llm.provider` in config.yaml names.
# Loaded automatically from .env — never pass a key on the command line, where
# it lands in shell history and `ps` output.

# llm.provider: anthropic
ANTHROPIC_API_KEY=sk-ant-...

# llm.provider: gemini
GEMINI_API_KEY=...
