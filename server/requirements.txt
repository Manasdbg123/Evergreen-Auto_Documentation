# Stage 1 — ingest, frames, change detection (no API key needed)
fastapi>=0.115
uvicorn[standard]
python-multipart
pydantic>=2
pyyaml
numpy
opencv-python-headless
pillow
imagehash
scikit-image
imageio-ffmpeg          # static ffmpeg binary; no sudo required

# Stage 2 — transcription (local CPU, no API)
faster-whisper

# Stages 6-7 — the model vendors. Only the one named by llm.provider is used
# at runtime; both are listed so switching needs no reinstall.
anthropic
google-genai

# Diff tier 2 — local embeddings, offline, no API key
sentence-transformers
# Optimal step assignment. Without it the diff degrades to greedy matching,
# which is order-dependent and measurably worse — see pipeline/diff.py.
scipy
